[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "causalDA"
version = "0.1.0"
description = "Causal Driven Attribution: synthetic data, graph evaluation, and attribution modeling"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [
  {name = "Your Name", email = "you@example.com"},
]

dependencies = [
  "numpy",
  "pandas",
  "networkx",
  "matplotlib"
]

[project.urls]
Homepage = "https://github.com/yourusername/causalDA"
Issues = "https://github.com/yourusername/causalDA/issues"
